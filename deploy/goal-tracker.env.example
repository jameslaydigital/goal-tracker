# Written to /etc/goal-tracker.env by the deploy script. No secrets today —
# sessions are random bearer tokens stored hashed server-side.
NODE_ENV=production
HOST=127.0.0.1
PORT=8080
DATA_DIR=/opt/goal-tracker/data
COOKIE_SECURE=1
